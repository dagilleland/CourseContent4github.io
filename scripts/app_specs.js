'use strict';
$(function () {

    // TODO: courseApp.appService
    //          should...
    //          Ensure app loads the app.json
    //          Report XHR errors to the console
    describe('appService', function () {
        /* App.Services
            contentServices
                should get valid JSON content
                should get HTML file content
         */
        it('should fail')

        // Function implementations (hoisted above)
        function justFail() {
            var test = undefined;
            expect(test).not.toBe(undefined);
        }
    });
});
